
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ТекстЗапроса.УстановитьТекст(КодДляЗапроса);
	Shell = Новый COMОбъект("WScript.Shell");
    Shell.SendKeys("^(A)");
    Shell.SendKeys("^(C)");
    
КонецПроцедуры
